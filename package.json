{
  "name": "monorepo-nectar",
  "version": "1.0.0",
  "license": "MIT",
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "volta": {
    "node": "14.15.5",
    "yarn": "1.22.10"
  },
  "dependencies": {
    "concurrently": "^6.0.0",
    "dotenv": "^8.2.0"
  },
  "scripts": {
    "start": "lerna run start --scope=@nectar/server",
    "build": "lerna run build --stream",
    "dev": "concurrently --kill-others \"lerna run dev --scope=@nectar/server\" \"lerna run start --scope=@nectar/components\""
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
